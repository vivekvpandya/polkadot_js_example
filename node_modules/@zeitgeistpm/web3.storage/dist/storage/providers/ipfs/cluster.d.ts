import * as Te from '@zeitgeistpm/utility/dist/taskeither';
import { IPFSClusterConfiguration } from './types';
/**
 * Response type for both pinning and unpinning the the ipfs cluster.
 */
export declare type IPFSClusterPinningResponse = {
    replication_factor_min: number;
    replication_factor_max: number;
    name: string;
    mode: string;
    shard_size: number;
    user_allocations: string;
    expire_at: string;
    metadata: string;
    pin_update: string;
    cid: string;
    type: number;
    allocations: Array<string>;
    max_depth: number;
    reference: string;
};
/**
 *
 * Pinns a cid to the cluster.
 */
export declare const pin: Te.TaskEither<Error, IPFSClusterPinningResponse, [cid: string, config: IPFSClusterConfiguration]>;
/**
 *
 * Unpinns a cid from the cluster.
 */
export declare const unpin: Te.TaskEither<Error, IPFSClusterPinningResponse, [cid: string, config: IPFSClusterConfiguration]>;
//# sourceMappingURL=cluster.d.ts.map