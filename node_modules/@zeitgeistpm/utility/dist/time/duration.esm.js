import ms from 'ms';
import { isString } from '@polkadot/util';

/**
 * Typeguard for duration strings.
 *
 * @param duration
 * @returns duration is Duration
 */
const isDuration = (duration) => Boolean(isString(duration) && duration.match(/[1-9]+ second|minute|hour|day|week|month/));
/**
 * Get duration in milliseconds.
 *
 * @param duration Duration
 * @returns number
 */
const asMs = (duration) => ms(duration);

export { asMs, isDuration };
//# sourceMappingURL=duration.esm.js.map
