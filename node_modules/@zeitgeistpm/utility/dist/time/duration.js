'use strict';

var ms = require('ms');
var util = require('@polkadot/util');

/**
 * Typeguard for duration strings.
 *
 * @param duration
 * @returns duration is Duration
 */
const isDuration = (duration) => Boolean(util.isString(duration) && duration.match(/[1-9]+ second|minute|hour|day|week|month/));
/**
 * Get duration in milliseconds.
 *
 * @param duration Duration
 * @returns number
 */
const asMs = (duration) => ms(duration);

exports.asMs = asMs;
exports.isDuration = isDuration;
//# sourceMappingURL=duration.js.map
