import { isNotNull } from '../null/index.esm.js';

/**
 * Create a NA value.
 *
 * @param reason string
 * @returns NA
 */
const na = (reason) => ({
    __type: sym,
    reason,
});
const sym = Symbol();
/**
 * Const symbold representing a not available value.
 */
const NA = na('Not available.');
/**
 * Typeguard for NA values.
 *
 * @param value unknown
 * @returns value is NA
 */
const isNA = (value) => typeof value === 'object' &&
    isNotNull(value) &&
    '__type' in value &&
    value['__type'] === sym;
/**
 * Typeguard to check if a value is available
 *
 * @param value T | NA
 * @returns value is T
 */
const isAvailable = (value) => !isNA(value);

export { NA, isAvailable, isNA, na };
//# sourceMappingURL=index.esm.js.map
