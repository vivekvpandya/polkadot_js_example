/**
 * Asserts an expresson and throw onError return value if falsy.
 */
const assert = (expression, onError) => {
    if (!expression)
        throw onError();
};

export { assert };
//# sourceMappingURL=index.esm.js.map
