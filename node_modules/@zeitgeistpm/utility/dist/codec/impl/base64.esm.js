import { codec } from '../index.esm.js';

/**
 * Encode and decode objects from json strings.
 * Simple wrapper around JSON.parse|stringify
 */
const Base64Codec = () => codec({
    encode: output => {
        try {
            return btoa(output);
        }
        catch (err) {
            return Buffer.from(output).toString('base64');
        }
    },
    decode: input => {
        try {
            return atob(input);
        }
        catch (err) {
            return Buffer.from(input).toString('utf8');
        }
    },
});

export { Base64Codec };
//# sourceMappingURL=base64.esm.js.map
