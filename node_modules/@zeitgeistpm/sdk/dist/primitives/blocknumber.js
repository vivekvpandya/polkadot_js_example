'use strict';

var util = require('@polkadot/util');
var superstruct = require('superstruct');

/**
 * Typeguard for block nunbers.
 *
 * @param n any
 * @returns  n is BlockNumber
 */
const isBlockNumber = (n) => util.isNumber(n);
/**
 * Io type for validation of block numbers.
 */
const IOBlockNumber = superstruct.define('BlockNumber', isBlockNumber);

exports.IOBlockNumber = IOBlockNumber;
exports.isBlockNumber = isBlockNumber;
//# sourceMappingURL=blocknumber.js.map
