'use strict';

var util = require('@polkadot/util');

const isRpcData = (object) => Boolean(object && util.isCodec(object));
const isIndexedData = (object) => Boolean(object && !isRpcData(object));

exports.isIndexedData = isIndexedData;
exports.isRpcData = isRpcData;
//# sourceMappingURL=data.js.map
