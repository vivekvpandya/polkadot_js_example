'use strict';

var util = require('@polkadot/util');
var superstruct = require('superstruct');

/**
 * Typeguard for market ids.
 *
 * @param n any
 * @returns  n is MarketId
 */
const isMarketId = (n) => util.isNumber(n);
/**
 * Io type for validation of market ids.
 */
const IOMarketId = superstruct.define('MarketId', isMarketId);

exports.IOMarketId = IOMarketId;
exports.isMarketId = isMarketId;
//# sourceMappingURL=marketid.js.map
