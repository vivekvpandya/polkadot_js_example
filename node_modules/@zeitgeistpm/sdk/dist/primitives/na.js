'use strict';

var _null = require('@zeitgeistpm/utility/dist/null');

/**
 * Create a NA value.
 *
 * @param reason string
 * @returns NA
 */
const na = (reason) => ({
    __type: sym,
    reason,
    toString: () => `NA(${reason})`,
});
const sym = Symbol();
/**
 * Const symbold representing a not available value.
 */
const NA = na('Not available.');
/**
 * Get the reason the value is not available.
 *
 * @param na NA
 * @returns string
 */
const reasonOf = (na) => na.reason;
/**
 * Typeguard for NA values.
 *
 * @param value unknown
 * @returns value is NA
 */
const isNA = (value) => typeof value === 'object' &&
    _null.isNotNull(value) &&
    '__type' in value &&
    value['__type'] === sym;
/**
 * Typeguard to check if a value is available
 *
 * @param value T | NA
 * @returns value is T
 */
const isAvailable = (value) => !isNA(value);

exports.NA = NA;
exports.isAvailable = isAvailable;
exports.isNA = isNA;
exports.na = na;
exports.reasonOf = reasonOf;
//# sourceMappingURL=na.js.map
