import { isNumber } from '@polkadot/util';
import { define } from 'superstruct';

/**
 * Typeguard for market ids.
 *
 * @param n any
 * @returns  n is MarketId
 */
const isMarketId = (n) => isNumber(n);
/**
 * Io type for validation of market ids.
 */
const IOMarketId = define('MarketId', isMarketId);

export { IOMarketId, isMarketId };
//# sourceMappingURL=marketid.esm.js.map
