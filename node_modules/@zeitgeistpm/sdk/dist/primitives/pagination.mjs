import { isObject, isNumber } from '@polkadot/util';

/**
 * Typeguard for pagination query objects.
 *
 * @param query any
 * @returns query is PaginationQuery
 */
const isPaginated = (query) => Boolean(query &&
    isObject(query) &&
    'offset' in query &&
    'limit' in query &&
    isNumber(query['offset']) &&
    isNumber(query['limit']));

export { isPaginated };
//# sourceMappingURL=pagination.mjs.map
