import { isCodec } from '@polkadot/util';

const isRpcData = (object) => Boolean(object && isCodec(object));
const isIndexedData = (object) => Boolean(object && !isRpcData(object));

export { isIndexedData, isRpcData };
//# sourceMappingURL=data.esm.js.map
