'use strict';

var util = require('@polkadot/util');

/**
 * Typeguard for pagination query objects.
 *
 * @param query any
 * @returns query is PaginationQuery
 */
const isPaginated = (query) => Boolean(query &&
    util.isObject(query) &&
    'offset' in query &&
    'limit' in query &&
    util.isNumber(query['offset']) &&
    util.isNumber(query['limit']));

exports.isPaginated = isPaginated;
//# sourceMappingURL=pagination.js.map
