import { isNumber } from '@polkadot/util';
import { define } from 'superstruct';

/**
 * Typeguard for block nunbers.
 *
 * @param n any
 * @returns  n is BlockNumber
 */
const isBlockNumber = (n) => isNumber(n);
/**
 * Io type for validation of block numbers.
 */
const IOBlockNumber = define('BlockNumber', isBlockNumber);

export { IOBlockNumber, isBlockNumber };
//# sourceMappingURL=blocknumber.esm.js.map
