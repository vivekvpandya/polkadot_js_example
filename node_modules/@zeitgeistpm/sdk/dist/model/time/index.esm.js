import { pfunc } from '@zeitgeistpm/utility/dist/pfunc';
import { isRpcContext } from '../../context/types.esm.js';
import { now, now$ } from './functions/now/index.esm.js';

const model = (ctx) => {
    if (isRpcContext(ctx)) {
        return {
            now: pfunc(() => now(ctx), {
                $: () => now$(ctx),
            }),
        };
    }
    return {};
};

export { model };
//# sourceMappingURL=index.esm.js.map
