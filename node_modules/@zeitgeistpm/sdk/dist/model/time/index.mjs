import { pfunc } from '@zeitgeistpm/utility/dist/pfunc';
import { isRpcContext } from '../../context/types.mjs';
import { now, now$ } from './functions/now/index.mjs';

const model = (ctx) => {
    if (isRpcContext(ctx)) {
        return {
            now: pfunc(() => now(ctx), {
                $: () => now$(ctx),
            }),
        };
    }
    return {};
};

export { model };
//# sourceMappingURL=index.mjs.map
