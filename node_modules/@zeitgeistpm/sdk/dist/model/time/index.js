'use strict';

var pfunc = require('@zeitgeistpm/utility/dist/pfunc');
var types = require('../../context/types.js');
var index = require('./functions/now/index.js');

const model = (ctx) => {
    if (types.isRpcContext(ctx)) {
        return {
            now: pfunc.pfunc(() => index.now(ctx), {
                $: () => index.now$(ctx),
            }),
        };
    }
    return {};
};

exports.model = model;
//# sourceMappingURL=index.js.map
