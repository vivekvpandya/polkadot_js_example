import { Context } from '../../../../context/types';
import { MetadataStorage } from '../../../../meta';
import { PoolList } from '../../poolslist';
import { PoolsListQuery } from '../../types';
/**
 * Query for a list of pools.
 * Query capabilities and returned data differentiates between a rpc and indexer context.
 *
 * @generic C - Context
 * @param context C - the context to query in
 * @param query ListQuery<C>
 * @returns Promise<PoolList<C>>
 */
export declare const listPools: <C extends Context<MS>, MS extends MetadataStorage<{
    __meta: "markets";
    slug: string;
    description: string;
    question: string;
    tags?: string[] | undefined;
    confidentialId?: string | undefined;
    img?: string | undefined;
    scalarType?: "number" | "date" | undefined;
    categories?: {
        name: string;
        img?: string | undefined;
        ticker?: string | undefined;
        color?: string | undefined;
    }[] | undefined;
}, import("../../../../meta").CommentMetadata>>(context: C, query: PoolsListQuery<C, MS>) => Promise<PoolList<C, MS>>;
//# sourceMappingURL=index.d.ts.map