'use strict';

var util = require('@polkadot/util');

/**
 * Typeguard for PoolGetByIdQuery
 *
 * @param query PoolGetQuery
 * @returns query is PoolGetByIdQuery
 */
const isPoolIdQuery = (query) => 'poolId' in query && util.isNumber(query.poolId);
/**
 * Typeguard for PoolGetByMarketIdQuery
 *
 * @param query PoolGetQuery
 * @returns query is PoolGetByMarketIdQuery
 */
const isMarketIdQuery = (query) => 'marketId' in query && util.isNumber(query.marketId);

exports.isMarketIdQuery = isMarketIdQuery;
exports.isPoolIdQuery = isPoolIdQuery;
//# sourceMappingURL=types.js.map
