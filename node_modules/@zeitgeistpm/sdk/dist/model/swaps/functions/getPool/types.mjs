import { isNumber } from '@polkadot/util';

/**
 * Typeguard for PoolGetByIdQuery
 *
 * @param query PoolGetQuery
 * @returns query is PoolGetByIdQuery
 */
const isPoolIdQuery = (query) => 'poolId' in query && isNumber(query.poolId);
/**
 * Typeguard for PoolGetByMarketIdQuery
 *
 * @param query PoolGetQuery
 * @returns query is PoolGetByMarketIdQuery
 */
const isMarketIdQuery = (query) => 'marketId' in query && isNumber(query.marketId);

export { isMarketIdQuery, isPoolIdQuery };
//# sourceMappingURL=types.mjs.map
