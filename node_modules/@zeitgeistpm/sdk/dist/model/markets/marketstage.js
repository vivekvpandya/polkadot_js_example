'use strict';

var infinity = require('@zeitgeistpm/utility/dist/infinity');

/**
 * Check if a stage is a InfiniteStage.
 *
 * @param stage MarketStage
 * @returns stage is InfiniteStage<any>
 */
const isInfinite = (stage) => infinity.isInfinity(stage.remainingTime) && infinity.isInfinity(stage.totalTime);

exports.isInfinite = isInfinite;
//# sourceMappingURL=marketstage.js.map
