import { isInfinity } from '@zeitgeistpm/utility/dist/infinity';

/**
 * Check if a stage is a InfiniteStage.
 *
 * @param stage MarketStage
 * @returns stage is InfiniteStage<any>
 */
const isInfinite = (stage) => isInfinity(stage.remainingTime) && isInfinity(stage.totalTime);

export { isInfinite };
//# sourceMappingURL=marketstage.mjs.map
