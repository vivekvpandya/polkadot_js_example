import { ping } from './ping.esm.js';

/**
 * Create the augmented api.
 *
 * @param gql GQLSdk
 * @returns Augments
 */
const augments = (gql) => {
    return {
        ping: () => ping(gql),
    };
};

export { augments };
//# sourceMappingURL=index.esm.js.map
