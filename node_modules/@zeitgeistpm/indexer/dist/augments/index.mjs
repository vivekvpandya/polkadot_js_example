import { ping } from './ping.mjs';

/**
 * Create the augmented api.
 *
 * @param gql GQLSdk
 * @returns Augments
 */
const augments = (gql) => {
    return {
        ping: () => ping(gql),
    };
};

export { augments };
//# sourceMappingURL=index.mjs.map
